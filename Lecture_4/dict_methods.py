# dictionary methods
dict={
    "name":"Suryansh",
    "rollNo":2302086,
    "age":20,
}
print(dict.keys()) # returns the keys
print(dict.values()) # returns the values
print(dict.items()) # returns (key,value) pairs as tuples
print(dict.get("name")) # returns value according to key
